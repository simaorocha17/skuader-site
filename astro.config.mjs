// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // domínio de produção (canonical + og:image)
  site: 'https://skuader.com',
});
